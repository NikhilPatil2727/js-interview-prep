
Prototype 👉 Child object borrows properties from its paretn object.


**Prototype chaining 👉 When a JavaScript object doesn’t find a property on itself, it looks for it in its prototype, then the prototype’s prototype, and so on—this lookup chain is called prototype chaining.**

