// undefined

// A variable is declared, but no value is assigned to it yet.

let a;
console.log(a); // undefined


// not defined

// 👉 The variable was never declared, so JavaScript doesn’t know it.

// Example:

console.log(b); // ReferenceError: b is not defined

